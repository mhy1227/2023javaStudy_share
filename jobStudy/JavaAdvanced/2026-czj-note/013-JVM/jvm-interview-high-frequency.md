# JVM 高频面试题清单

## 1. 这份文档怎么用

这一篇只负责收拢 JVM 高频问题，不展开长答案。

建议用法：

1. 先按模块过一遍问题。
2. 哪些答不顺，就回对应主线文档复习。
3. 最后再结合 `jvm-interview-answers.md` 做口语化复述。

## 2. 基础认知

- 什么是 JVM？
- 为什么 Java 要强调 JVM？
- JVM 和 JDK、JRE 怎么区分？
- 为什么说 Java 能跨平台和 JVM 有关系？

## 3. 内存结构

- JVM 运行时内存结构有哪些？
- 堆和栈有什么区别？
- 方法区怎么理解？
- 程序计数器是干什么的？
- 本地方法栈怎么理解？
- 为什么面试总爱问堆、栈、方法区？

## 4. GC

- 什么是 GC？
- 为什么 JVM 需要 GC？
- JVM 怎么判断对象是不是垃圾？
- 为什么不主讲引用计数？
- 什么是可达性分析？
- 什么是 GC Roots？
- 常见垃圾回收算法有哪些？
- 标记清除、复制、标记整理怎么区分？
- 为什么要有分代思想？
- 新生代和老年代怎么理解？
- 什么是 Stop-The-World？

## 5. 垃圾回收器

- 常见垃圾回收器知道哪些？
- CMS 怎么理解？
- G1 怎么理解？
- CMS 和 G1 怎么简单区分？
- Minor GC 和 Full GC 有什么区别？
- 为什么 Full GC 频繁很危险？

## 6. 类加载

- 什么是类加载？
- 类加载过程有哪些步骤？
- 加载、验证、准备、解析、初始化分别做什么？
- 什么是类加载器？
- 常见类加载器知道哪些？
- 什么是双亲委派？
- 为什么要有双亲委派？
- 如果没有双亲委派会怎么样？

## 7. JMM 与 volatile

- 什么是 JMM？
- 为什么要有 JMM？
- `volatile` 有什么作用？
- `volatile` 能不能保证线程安全？
- 为什么 `volatile` 不能保证 `i++`？
- happens-before 是什么？
- 你至少知道哪些 happens-before 规则？
- `volatile` 和 `synchronized` 怎么区分？

## 8. 对象与引用

- `new` 一个对象大概发生了什么？
- 对象主要分配在哪？
- 强软弱虚引用怎么区分？
- 什么场景下会用到软引用、弱引用？

## 9. OOM 与排查

- 什么是 OOM？
- 什么是内存泄漏？
- 内存泄漏和 OOM 有什么区别？
- 常见 OOM 类型有哪些？
- 堆溢出和栈溢出怎么区分？
- 元空间溢出怎么理解？
- 内存泄漏常见场景有哪些？
- OOM 怎么排查？

## 10. 调优与线上问题

- JVM 调优一般怎么回答？
- Full GC 频繁怎么排查？
- CPU 飙高怎么查？
- 常见 JVM 工具知道哪些？
- 为什么说调优不是只改参数？
- 如果没真实调优经验，怎么答更稳？

## 11. 最后一轮大收口

- 你怎么整体理解 JVM？
- JVM 这一章最核心的主线是什么？
- 如果只用 1 分钟讲 JVM，你会怎么讲？
