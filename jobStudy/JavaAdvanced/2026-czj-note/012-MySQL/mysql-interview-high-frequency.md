# MySQL 高频面试题清单

## 1. 这份文档怎么用

这份文档只负责收拢问题，不展开长答案。

建议用法：

1. 先按模块过一遍问题。
2. 哪些答不顺，就回到对应主线文档复习。
3. 最后再结合 `mysql-interview-answers.md` 做口语化复述。

## 2. 基础认知

- MySQL 是什么？为什么 Java 后端项目里经常用它？
- MySQL 和 Redis 的分工是什么？
- MySQL 为什么是关系型数据库？
- 为什么项目里一般不会只用 Redis，不用 MySQL？
- MySQL Server 层和存储引擎层怎么理解？
- InnoDB 为什么比 MyISAM 更常用？

## 3. 索引

- 什么是索引？
- 为什么索引能加快查询？
- 为什么 MySQL 索引常说 B+ 树？
- 聚簇索引和二级索引有什么区别？
- 什么是回表？
- 什么是覆盖索引？
- 主键索引、普通索引、唯一索引、联合索引分别是什么？
- 联合索引的最左前缀原则是什么？
- 什么情况下索引会失效？
- 为什么有索引，SQL 还是会慢？
- 什么情况下适合建索引？
- 什么情况下不适合建索引？
- 为什么索引不是越多越好？

## 4. 事务与 MVCC

- 什么是事务？
- ACID 是什么？
- 四种隔离级别分别是什么？
- MySQL 默认隔离级别是什么？
- 什么是脏读、不可重复读、幻读？
- MVCC 是什么？
- MVCC 想解决什么问题？
- MVCC 依赖什么机制？
- 什么是 Read View？
- 快照读和当前读有什么区别？
- 锁和 MVCC 是什么关系？

## 5. 锁

- 为什么数据库需要锁？
- 表锁和行锁有什么区别？
- InnoDB 行锁为什么和索引有关？
- 共享锁和排他锁是什么？
- 什么是意向锁？
- 什么是记录锁、间隙锁、临键锁？
- 间隙锁和临键锁主要解决什么问题？
- 什么是死锁？
- 死锁怎么避免？
- MySQL 有哪些常见锁？

## 6. 日志

- redo log、undo log、binlog 分别是什么？
- redo log 主要解决什么问题？
- undo log 为什么和 MVCC 有关系？
- binlog 为什么和主从复制有关？
- redo log 和 binlog 有什么区别？
- 为什么 MySQL 既有 redo log，又有 binlog？
- 什么是两阶段提交？
- 为什么两阶段提交很重要？

## 7. 执行流程

- 一条 SQL 进入 MySQL 后大概怎么执行？
- 连接器、解析器、优化器、执行器分别做什么？
- 查询 SQL 和更新 SQL 的执行流程有什么不同？
- 优化器为什么不一定选你想象中的索引？
- 执行流程为什么会和索引、锁、日志都有关？

## 8. EXPLAIN 与慢 SQL 排查

- EXPLAIN 是干什么的？
- EXPLAIN 里重点看哪些字段？
- `type`、`key`、`rows`、`Extra` 分别怎么看？
- `possible_keys` 和 `key` 有什么区别？
- `Using index`、`Using temporary`、`Using filesort` 分别说明什么？
- 慢 SQL 一般怎么排查？
- 为什么不能只看有没有走索引？
- 为什么慢 SQL 排查不能只回答“加索引”？

## 9. 场景题

- 项目里某条 SQL 很慢，你会怎么排查？
- 订单表更新很慢，你先看什么？
- 查询走了索引但还是慢，可能原因有哪些？
- 高并发更新库存时，MySQL 层面会关注哪些问题？
- 如果面试官追问主从一致性和事务提交，你会怎么接？

## 10. 当前阶段最该先背哪些题

优先级最高的一批：

1. 什么是索引，为什么是 B+ 树。
2. 聚簇索引、二级索引、回表、覆盖索引。
3. 事务、ACID、隔离级别、MVCC。
4. 行锁、间隙锁、临键锁。
5. redo log、undo log、binlog、两阶段提交。
6. 一条 SQL 怎么执行。
7. EXPLAIN 看什么，慢 SQL 怎么排查。