# MQ 情景深挖问题清单

## 1. 使用方式

这份文档只整理 MQ 情景问题，不写详细回答。

后续可以再逐个补：

- 消息可靠性回答
- 消费幂等回答
- 顺序消息回答
- 消息积压排查回答
- 项目场景回答

## 2. 消息可靠性

- MQ 怎么保证消息可达？
- 消息从生产者到 Broker 失败怎么办？
- 生产者发送消息超时，但消息可能已经到 Broker，怎么办？
- 生产者发送成功，但本地事务失败怎么办？
- 本地事务成功，但消息发送失败怎么办？
- 消息到了 Broker，但 Broker 宕机怎么办？
- 消息没有被消费者消费，怎么发现？
- 消息消费失败后怎么重试？
- 重试多次还是失败怎么办？
- 死信队列一般用来处理什么？

## 3. 消息不丢问题

- MQ 怎么保证消息不丢？
- 哪些环节可能导致消息丢失？
- 生产者丢消息怎么处理？
- Broker 丢消息怎么处理？
- 消费者丢消息怎么处理？
- 消费者业务处理成功但 ack 失败，会发生什么？
- 消费者 ack 成功但业务还没处理完，会有什么问题？
- 如果消息丢了，怎么补偿？

## 4. 重复消息与幂等

- 为什么 MQ 会出现重复消息？
- 消息重复消费怎么办？
- 怎么保证消费者幂等？
- 幂等和去重有什么区别？
- 只靠 MQ messageId 做幂等够不够？
- 为什么更推荐业务唯一键做幂等？
- 数据库唯一索引怎么做幂等？
- Redis setnx 怎么做幂等？
- 状态机怎么做幂等？
- 重复支付成功消息怎么处理？
- 重复扣库存消息怎么处理？

## 5. 消息唯一性

- 怎么给消息生成唯一标识？
- 消息唯一 ID 应该由谁生成？
- 业务唯一键和 MQ messageId 有什么区别？
- 如果同一个业务请求发送了两条消息，怎么识别？
- 消息唯一性和消费幂等是什么关系？
- 全局唯一 ID 可以用雪花算法吗？
- 消息去重记录要保存多久？

## 6. 顺序消息

- MQ 怎么保证消息顺序？
- 全局顺序和局部顺序有什么区别？
- 为什么一般只保证同一业务 key 的局部顺序？
- 同一个订单的消息怎么保证顺序？
- 消费者并发消费会不会破坏顺序？
- 如果前一条消息消费失败，后一条消息怎么办？
- 顺序消息会不会影响吞吐量？
- 订单状态消息乱序了怎么办？

## 7. 消息积压

- 消息积压是什么？
- 消息为什么会积压？
- 消费者处理慢怎么办？
- 消费者实例太少怎么办？
- 下游数据库慢导致积压怎么办？
- 消费失败不断重试导致积压怎么办？
- 突发流量太高导致积压怎么办？
- 消息积压时能不能临时扩容消费者？
- 消息积压时怎么保证不丢消息？
- 消息积压太久会不会过期？

## 8. 消息延迟

- MQ 为什么会有延迟？
- 消息延迟太高怎么排查？
- 削峰为什么会带来延迟？
- 延迟消息适合什么场景？
- 订单超时取消可以用延迟消息吗？
- 延迟消息失败怎么补偿？

## 9. 事务消息与本地消息表

- 什么是事务消息？
- 什么是本地消息表？
- 本地事务和消息发送怎么保证一致？
- 本地消息表定时补偿怎么做？
- 事务消息和本地消息表怎么选？
- 如果消息表补偿任务挂了怎么办？
- 消息表会不会越来越大？

## 10. MQ 和业务场景

- 下单后扣库存用 MQ 合适吗？
- 支付成功后通知订单服务用 MQ 合适吗？
- 文件上传完成后异步转码可以用 MQ 吗？
- 发短信、发邮件适合用 MQ 吗？
- 日志、统计、埋点适合用 MQ 吗？
- 哪些场景不适合用 MQ？

## 11. MQ 和分布式事务

- MQ 怎么实现最终一致？
- MQ 和 Seata 怎么选？
- 为什么有些场景不用 Seata，而用 MQ？
- MQ 最终一致失败怎么办？
- 补偿任务多次失败怎么办？
- 人工补偿怎么设计？
- 怎么保证补偿逻辑幂等？

## 12. MQ 排查题

- 线上发现消息没消费，怎么排查？
- 线上发现消息重复消费，怎么排查？
- 线上发现消息积压，怎么排查？
- 线上发现消费很慢，怎么排查？
- 消费者日志没有打印，但 MQ 显示消息已投递，怎么排查？
- 消费者一直重启，会影响消息消费吗？
- MQ Broker 异常时，业务系统怎么降级？

## 13. 一句话总结

MQ 情景题核心是围绕“可靠投递、重复消费、幂等、顺序、积压、补偿、排查”这条线展开。
